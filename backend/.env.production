# Backend Environment Variables for WorkLog - Production

# Server Configuration
PORT=3000
NODE_ENV=production

# Database (Vercel uses serverless functions, so use /tmp for temporary storage)
DATABASE_PATH=/tmp/worklog.sqlite

# JWT Secret (Set this as environment variable in Vercel)
JWT_SECRET=your-production-jwt-secret-key

# CORS Origins (Update with your actual frontend URL)
CORS_ORIGINS=https://worklog-o9uzb6jxk-dev-shotas-projects.vercel.app

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100